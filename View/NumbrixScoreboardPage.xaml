<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NumbrixGame.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Custom="using:Microsoft.Toolkit.Uwp.UI.Controls"
    x:Class="NumbrixGame.View.NumbrixScoreboardPage"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <Custom:DataGrid
            HorizontalAlignment="Center"
            Margin="0,158,0,0"
            VerticalAlignment="Top"
            Height="auto"
            Width="auto"
            ItemsSource="{x:Bind numbrixScoreBoardViewModel.PlayerScores, Mode=TwoWay}"
            CanUserReorderColumns="False"
            CanUserResizeColumns="True"
            CanUserSortColumns="True"
            HeadersVisibility="Column"
            SelectionMode="Single"
            GridLinesVisibility="All"
            AutoGenerateColumns="False"
            AreRowDetailsFrozen="True"
            Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <Custom:DataGrid.Columns>
                <Custom:DataGridTextColumn Header="Username"
                                             Binding="{Binding Username}"
                                             IsReadOnly="True"
                                             Tag="Username"/>
                <Custom:DataGridTextColumn Header="PuzzleNumber"
                                             Binding="{Binding PuzzleNumber}"
                                             IsReadOnly="True"
                                             Tag="PuzzleNumber"/>
                <Custom:DataGridTextColumn Header="Time Taken"
                                             Binding="{Binding TimeTaken}"
                                             IsReadOnly="True"
                                             Tag="Time Taken"/>


            </Custom:DataGrid.Columns>
        </Custom:DataGrid>
        <Button Content="Main Menu" Margin="769,100,0,0" VerticalAlignment="Top" Click="GoBack" Width="145"/>
        <Button Content="Clear Score Board" Margin="587,100,0,0" VerticalAlignment="Top" Click="ClearScoreBoard" Width="145"/>

    </Grid>
</Page>
